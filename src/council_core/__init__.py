"""Council Core — a generic multi-model council engine.

Domain behavior is supplied by packs (see ``packs/``); the engine itself is
domain-neutral. Scaffolding stage — see PLAN.md for the build order.
"""

__version__ = "0.0.1"
